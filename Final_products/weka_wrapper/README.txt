How to use:

In the command line add the arguments you want. -m with a model for a model and -f with an arff file for instances. 
  The program will then load the model and predict the outcome of the instances. The outcome will be printed in your terminal. 
